{
  "name": "sentinel-outbound-webhook-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Receives SQS-delivered webhook payloads and POSTs them to subscriber URLs.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --format=cjs --minify",
    "package": "npm run build && cd dist && zip -q -r ../dist/handler.zip index.js"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
